<!DOCTYPE html>
<html lang="en">
<head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Sign Up</title>
        <link rel="stylesheet" href="/css/main.css">
        <link rel="stylesheet" href="/css/auth.css">
</head>
<body>

    <%- include('../layout/header')%>

    <main class="main-content">
        <section class="card auth-card" style="max-width:560px;margin:40px auto;padding:24px;">
            <h2><%= t ? t('auth.signUp') : 'Sign Up' %></h2>
            <p class="subtle"><%= t ? t('auth.createAccountHelp') : 'Create an account to start sending money' %></p>
            <form class="authenticate-form" action="/register" method="post">
                <div class="form-row">
                    <label for="username">Full name</label>
                    <input id="username" name="username" type="text" placeholder="<%= t ? t('placeholders.username') : 'John Doe' %>" required>
                </div>
                <div class="form-row">
                    <label for="email">Email</label>
                    <input id="email" name="email" type="email" placeholder="<%= t ? t('placeholders.emailExample') : 'abc@gmail.com' %>" required>
                </div>
                <div class="form-row">
                    <label for="pwd">Password</label>
                    <div style="display:flex;gap:8px;align-items:center">
                        <input id="pwd" name="password" type="password" placeholder="<%= t ? t('placeholders.password') : 'Your password' %>" required style="flex:1">
                        <button type="button" class="toggle-password" data-target="#pwd">Show</button>
                    </div>
                </div>
                <div class="form-row">
                    <label for="phone">Phone (optional)</label>
                    <input id="phone" name="phone" type="text" placeholder="<%= t ? t('placeholders.phone') : '+84 9xx xxx xxx' %>">
                </div>
                <div class="btn-row">
                    <button type="submit" class="btn"><%= t ? t('auth.signUp') : 'Sign Up' %></button>
                    <a class="small-link" href="/login"><%= t ? t('auth.login') : 'Log in' %></a>
                </div>
            </form>
        </section>
    </main>

    <%- include('../layout/footer')%>

    <script src="/js/auth.js" defer></script>
    <script src="/js/main.js" defer></script>

</body>
</html>

